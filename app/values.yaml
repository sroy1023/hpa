replicaCount: 1

image:
  repository: 4oh4/kubernetes-fastapi
  tag: 1.0.0
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""


  ## Specifies whether RBAC rules should be created
 

## Pods Service Account
## ref: https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-accou
  ## Specifies whether a ServiceAccount should be created
  ##
  
  ## The name of the ServiceAccount to use.
  ## If not set and create is true, a name is generated using the common.names.fullname template
  # name:
  ## Automount API credentials for a service account.
  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/#use-the-default-service-account-to-access-the-api-se


## API service parameter
  ## Specifies whether the v1beta1.metrics.k8s.io API service should be created
  ## This is still necessary up to at least k8s version >= 1.21, but depends on vendors and 

# Specifies if metrics-server should be started in hostNetwork mode.
#
# You would require this enabled i
